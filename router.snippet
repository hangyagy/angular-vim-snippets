'use strict';

var express    = require('express');
var controller = require('./${1}.controller');
var router     = express.Router();

router.post('/', controller.index);${2}

module.exports = router;
